logging:
  level:
    org.hibernate: INFO
    org.springframework.r2dbc: INFO

spring:
  data:
    redis:
      host: ${REDIS_HOST:redis-master}
      port: ${REDIS_PORT:6379}
      connect-timeout: 5
      timeout: 5
      shutdown-timeout: 5
